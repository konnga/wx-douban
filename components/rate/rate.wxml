<view class="rate-wrap font-small rate-class">
  <view class="star-wrap">
    <text
      wx:for="{{starsArr}}"
      wx:key="index"
      class="{{item}}"
    >
    </text>
  </view>
  <text class="score">{{score}}</text>
</view>